apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: microservices
  labels:
    app: users-service
spec:
  type: ClusterIP
  selector:
    app: users-service
  ports:
    - port: 80
      targetPort: 80
