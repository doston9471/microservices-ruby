apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: users-service-data
  namespace: microservices
  labels:
    app: users-service
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
